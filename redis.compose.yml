services:
  redis:
    image: redis:alpine
    restart: unless-stopped
    command: ['redis-server', '--port', '6379']
    healthcheck:
      test: ['CMD', 'redis-cli', '-p', '6379', 'ping']
      start_period: 30s
      start_interval: 0.5s
      interval: 20s
      timeout: 5s
      retries: 5
